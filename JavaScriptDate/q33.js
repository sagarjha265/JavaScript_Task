function emptyArray(originalArray) {
    return []; // Returns a new empty array
}

// Example usage
let originalArray = [1, 2, 3];
let newArray = emptyArray(originalArray);
console.log(originalArray); // [1, 2, 3]
console.log(newArray); // []
