function Find(b , n){

    let result = 1;
    for(let i=1 ; i<= n ; i++){
         result = result*b;
    }
    return result;

}
let  b = parseInt(prompt("enter the value of b"));

let n = parseInt(prompt("Enter the value of n"));

const ans = Find(b , n);
console.log(ans);